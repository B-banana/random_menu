package utils;

import javax.swing.JFrame;

public interface Frame {
	
	/**
	 *  @param jFrame 프레임 기본 설정
	 *  @return
	 */
	
	JFrame setFrameSettings(JFrame jFrame);

}


// @param : 태그는 매개 변수에 대한 설명을 표시 할 때 사용한다.
// @return : 태그는 반환 값에 대한 설명(데이터 유형 및 범위 등)을 표시 할 때 사용한다.

